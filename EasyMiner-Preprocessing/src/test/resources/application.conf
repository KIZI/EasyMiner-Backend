akka {
  actor {
    debug {
      receive = on
      autoreceive = on
      fsm = on
    }
  }
}
db {
  server = ${TEST_DB_SERVER}
  user = ${TEST_DB_USER}
  password = ${TEST_DB_PASSWORD}
  name = ${TEST_DB_NAME}
}
hive {
  server = ${TEST_HIVE_SERVER}
  port = ${TEST_HIVE_PORT}
  user = ${TEST_HIVE_USER}
  name = ${TEST_HIVE_NAME}
}